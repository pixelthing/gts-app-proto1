<!-- SLIDE 2: top news -->
<div class="proto__slide proto__slide__1 proto__slide--in js-protoSlide js-protoSlide3Editor">
  
  <header class="proto__slide__bar proto__slide__bar--in js-protoSlideBackBtn">
    <span class="proto__slide__bar__back"></span>
    <h1 class="proto__slide__bar__title">editor’s picks</h1>
  </header> 

  {{#each news}}
    {{#lt @index 12}}
      <article class="proto__item">
        <header class="proto__item__header">
          <img class="proto__item__thumb">
        </header>
        <h2 class="proto__headline">
          <span class="proto__date">{{date}}</span>
          {{title}}
        </h2>
        <h4 class="proto__subhead">
          {{intro}} 
        </h4>
      </article>
    {{/lt}}
  {{/each}}

</div>